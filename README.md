# MyTicTacTeo
This HTML code creates a simple **Tic Tac Toe game**. It includes a title, navigation bar, a 3x3 game board, and displays game info like player turns. A reset button restarts the game. External CSS handles styling, and JavaScript (`index.js`) adds game logic and interactivity.
